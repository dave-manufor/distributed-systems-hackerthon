{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "{{ .Release.Name }}-web-service"
  },
  "spec": {
    "type": "ClusterIP",
    "selector": {
      "app": "web"
    },
    "ports": [
      {
        "port": 8080,
        "targetPort": "web-http-port"
      }
    ]
  }
}
